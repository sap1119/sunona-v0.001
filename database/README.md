# Database Package

This package contains all database-related modules for the Sunona platform.

## Structure

- `connection.py` - Database connection and session management
- `models.py` - SQLAlchemy ORM models
- `migrations/` - Alembic migration scripts
